version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install

  build:
    commands:
      - npm run build
      - ls -la
      - ls -la dist

  post_build:
    commands:
      - aws s3 sync dist/ s3://basu.work-space.co.in --delete